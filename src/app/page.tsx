import { theme } from '../style/theme';

export default function Home() {
  return (
    <div>
      <h1 className={theme.font.sizes.medium}>
        Store it - the only storage solution you need.
      </h1>
    </div>
  );
}
